# Contributing

- Keep all code and comments in English.
- Never commit secrets. Use `.env.example` for documentation only.
- Pin image versions. Do not use `latest`.
- Prefer idempotent scripts: safe to re-run without breaking a running server.
